# Pinrail — generated upstream import

**Generated upstream import; do not edit; not the consumer branch.**

This root commit establishes shared ancestry between the generated
`upstream-import` branch and the downstream `main` branch. No GPUI source has
been imported at this bootstrap checkpoint. The importer replaces this file
mechanically with the pinned upstream provenance and distribution notices.

Use `main` for Pinrail development and consumption. Runtime changes belong only
on `main`; upstream snapshots enter `upstream-import` through reviewed imports.
